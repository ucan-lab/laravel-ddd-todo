{
    "private": true,
    "scripts": {
        "dev": "vite",
        "build": "vite build"
    },
    "devDependencies": {
        "axios": "^1.6.2",
        "laravel-vite-plugin": "^0.8.0",
        "vite": "^4.0.0"
    },
    "engines": {
        "node": "21.x",
        "yarn": "Don't use yarn, Use npm",
        "pnpm": "Don't use pnpm, Use npm"
    }
}
